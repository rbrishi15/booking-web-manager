import { describe, test } from "vitest";

// Owner: Neoh (liang799) — /app/sessions
describe("UC2-03c Cancel Session", () => {
  test.todo("soft-cancels via Booker.cancel(), never a hard delete");
  test.todo("refunds all committed participants on cancellation");
});
