/** UUIDs are validated at the application boundary. */
export type UUID = string;

/** Sport and region vocabularies are supplied by the application. */
export type Sport = string;
export type Region = string;
