/** UUIDs are validated at the use-case boundary. */
export type UUID = string;

/** Sport and region vocabularies are supplied by use cases. */
export type Sport = string;
export type Region = string;
